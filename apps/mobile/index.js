import { registerRootComponent } from 'expo';
import App from './App';

// Entry point. registerRootComponent calls AppRegistry.registerComponent and
// ensures the environment is set up for both Expo Go and a native dev client.
registerRootComponent(App);
